import { ConfirmClickDirective } from './confirm-click.directive';

describe('ConfirmClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmClickDirective();
    expect(directive).toBeTruthy();
  });
});
